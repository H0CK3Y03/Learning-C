// comments are used by putting // for single line comments or
/*
    For multi-line comments
    They are ignored by the compiler, they are used for notes for other devs
*/

#include <stdio.h>

int main(){

    /* escape sequence = character combination consisting of a backslash \
                         followed by a letter or combination of digits.
                         they specify actions within a line or string text.
                         \n = newline
                         \t = tab
    
    */

    
    // printf("I\nlike\npizza!\n");
    // printf("1\t2\t3\n4\t5\t6\n7\t8\t9");
    printf("\"I like pizza\" - Abraham Lincoln probably"); // Placing a backslash before quotes allows us to print quotes as output instead of them acting as string determiners

    return 0;
}